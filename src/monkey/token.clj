(ns monkey.token)

(def TOKENS {:illegal "ILLEGAL"
             :eof "EOF"
             :ident "IDENT"
             :int "INT"
             :assign "="
             :plus "+"
             :comma ","
             :semicolon ";"
             :lparen "("
             :rparen ")"
             :lbrace "{"
             :rbrace "}"
             :function "FUNCTION"
             :let "LET"})
